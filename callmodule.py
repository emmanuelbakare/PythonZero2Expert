# import testmodule

# testmodule.
# testmodule.greetMe("Darin") 
# print(testmodule.key)

from testmodule import simple

simple()

# The current directory first.
# If 1 fails, it looks at PYTHONPATH variable in the environment variable
# If 1 and 2 fails it looks at the default path which is usually /usr/local/lib/python/. On unix and python installation path on windows

import math 

 