#abs()
#abs(-7) #returns 7



#enumerate

list1=["body","girl","men","women"]
for k,i in enumerate(list1):
    print(k,":",i)








# ALL
# all([1,34,5])  #returns True
# all ([0,23,4]) #returns false
# all([‘’,’name’,’age’]) #returns false

# ANY 
# any([0,0,23])  #returns True

#ASCII
# ascii('ș')  #returns "'\\u0219'“

# BIN(int) – converts integer to binary
# bin(19) #return  '0b10011'

# BOOL(int) – convert integer to Boolean. 0 and ‘’ is false
# bool(10) #returns True
# bool(0) #returns False

# CALLABLE(obj) – tell us if an object can be called.
# import math
# callable(math.ceil)  #return True

# CHR(val) – returns character of the entered value
# chr(97)  #returns ‘a’

# DIVMOD(numerator, denominator)= returns a tuple of a division and is remainder
# divmod(17,5)  #returns (3, 2)

# ENUMERATE(iterable) – add a counter to iterable
# list1=["boy","girl","men","women"]
# for i,k in enumerate(list1):print(i,":",k)


# EVAL(string) – evaluate a string as an expression
# eval(‘7+2’)  return 9

# POW(a,b) – returns the value of a raise to power b (a**b or a*a*a)
# Power(4,3) # returns 64 (4*4*4)


# OTHERS INCLUDES
# bytearray(4), bytes(5), classmethod(), 
# compile(), complex(), delattr(), dict(), dir(), 
# exec(), eval(), filter(), float(), format(), 
# frozenset(), getattr() ,globals(), hasattr(), 
# hash(), help(), hex(), id() ,input(), int(),
# isinstance() issubclass(), iter(), len(), 
# list(), locals(), map(), max(), memoryview(), 
# next(), object(), oct(), open()ord(), pow(), 
# print(), range(), property(), repr(), reversed() 
# etc




